package infernal.mod.sound;

import infernal.mod.InfernalMod;
import net.minecraft.block.Block;
import net.minecraft.registry.Registries;
import net.minecraft.registry.Registry;
import net.minecraft.sound.SoundEvent;
import net.minecraft.sound.SoundEvents;
import net.minecraft.util.Identifier;

public class ModSoundEvents {
    /*
    // register handbell sound
    public static final Block HANDBELL_USE = registerSoundEvent("handbell_use");

    private static Block registerSoundEvent(String name) {
        return Registry.register(
                Registries.SOUND_EVENT,
                new Identifier(InfernalMod.MOD_ID, name),
                new net.minecraft.sound.SoundEvent()
        );
    }
    */

}
